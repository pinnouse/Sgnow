{
  "name": "sgnow",
  "version": "0.1.0",
  "description": "",
  "main": "src/main.js",
  "author": "pinnouse <cupofpho@gmail.com>",
  "license": "MIT",
  "scripts": {
    "install": "neon build --release",
    "start": "electron .",
    "build": "electron-build-env neon build --release"
  },
  "dependencies": {
    "electron-build-env": "^0.2.0",
    "neon-cli": "^0.2.0"
  },
  "devDependencies": {
    "electron": "^5.0.7"
  }
}
